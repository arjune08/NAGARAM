[project]
name = "urban-pulse-ai"
version = "1.0.0"
description = "UrbanPulse AI - Urban Infrastructure Intelligence Platform"
requires-python = ">=3.11,<3.13"
dependencies = [
    "Flask",
    "Flask-SQLAlchemy",
    "Flask-CORS",
    "Werkzeug",
    "SQLAlchemy",
]

[tool.vercel]
entrypoint = "run:app"
